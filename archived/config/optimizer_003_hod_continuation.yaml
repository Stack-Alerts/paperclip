# Optimizer Configuration for Strategy: HOD Continuation
# Strategy Number: 003
# Category: 
# Auto-generated by Strategy Builder v1.0
# Date: 2026-01-11 16:20:37

strategy:
  name: "HOD Continuation"
  number: 3
  category: ""
  side: "LONG"  # Trade direction: SHORT or LONG
  file: "strategy_003_hod_continuation.py"
  class: "StrategyHodContinuation"

# Parameter Optimization Ranges
parameters:
  # Confluence threshold
  min_confluence:
    type: "int"
    min: 40
    max: 80
    step: 5
    default: 60
  
  # Block weights (optimizable)
  block_weights:
    hod:
      type: "int"
      min: 10
      max: 30
      step: 5
      default: 20
      enabled: true
    hod:
      type: "int"
      min: 10
      max: 30
      step: 5
      default: 20
      enabled: true
    stochastic_rsi:
      type: "int"
      min: 15
      max: 35
      step: 5
      default: 25
      enabled: true
    range_liquidity:
      type: "int"
      min: 5
      max: 25
      step: 5
      default: 15
      enabled: true
    how:
      type: "int"
      min: 10
      max: 30
      step: 5
      default: 20
      enabled: true
  
  # Signal roles (TEST_ALL signals will be tested in all configurations)
  signal_permutations:

# Walk-Forward Test Configuration
walk_forward:
  enabled: true
  
  # Training/testing split
  training_window_days: 90
  testing_window_days: 30
  step_days: 30
  
  # Data settings
  data_file: "data/raw/BTC_USDT_PERP_15m.csv"
  timeframe: "15min"
  minimum_bars: 200
  
  # Performance metrics to optimize
  optimization_target: "sharpe_ratio"  # sharpe_ratio, profit_factor, win_rate, net_pnl
  
  # Constraints
  constraints:
    min_trades: 5  # Minimum trades in test period
    max_drawdown_pct: 25.0  # Maximum allowed drawdown
    min_win_rate_pct: 30.0  # Minimum win rate
    min_risk_reward: 1.4  # Minimum R:R ratio (CRITICAL: 1.2-1.5 for Adaptive SL v2.0 with 0.9% SL)

# Backtest Configuration
backtest:
  # Capital settings
  initial_capital: 20000.0  # Starting capital from strategy configuration
  capital_allocation_pct: 100.0
  
  # Risk management
  max_leverage: 15.0
  risk_per_trade_pct: 8.0
  max_position_size: 1.0  # BTC
  
  # Take Profit Mode (CRITICAL: Determines TP calculation method)
  tp_mode: "HYBRID"  # PERCENTAGE (fixed), FIBONACCI (dynamic), HYBRID (adaptive)
  
  # Fees
  maker_fee_pct: 0.02
  taker_fee_pct: 0.05
  
  # Execution
  slippage_pct: 0.05
  
  # Adaptive SL v2.0 Configuration (from Strategy Builder)
  adaptive_sl:
    enabled: true  # Always enabled for institutional-grade risk management
    volatility_lookback: 20
    volatility_multiplier: 1.2
    absolute_min_sl_pct: 0.7
    absolute_max_sl_pct: 2.0
    use_delayed_sl: true
    delay_bars: 2
    emergency_sl_pct: 3.5
    use_structure_sl: true
    structure_sources: ["swing_points", "supply_demand", "fibonacci"]
  
  # Dynamic TP Configuration (placeholder - will be optimization parameter)
  # The optimizer will test multiple TP configurations
  take_profit_levels:
    - level: 1.5  # 1.5R
      size_pct: 50.0
    - level: 3.0  # 3.0R
      size_pct: 30.0
    - level: 5.0  # 5.0R
      size_pct: 20.0

# Optimization Settings
optimization:
  # Search strategy
  method: "grid_search"  # grid_search, random_search, bayesian
  
  # Grid search settings
  grid_search:
    max_combinations: 500  # Limit total combinations
    parallel_jobs: 4  # Number of parallel backtests
  
  # Random search settings (if using random_search)
  random_search:
    n_iterations: 200
    random_seed: 42
  
  # Bayesian optimization settings (if using bayesian)
  bayesian:
    n_iterations: 100
    n_initial_points: 20
    acquisition_function: "ei"  # Expected Improvement

# Results Configuration
results:
  # Output directory
  output_dir: "optimization_results/strategy_003_hod_continuation"
  
  # Save settings
  save_top_n: 10  # Save top N configurations
  save_equity_curves: true
  save_trade_log: true
  save_detailed_report: true
  
  # Report format
  report_format: "markdown"  # markdown, html, pdf

# Validation Settings
validation:
  # Cross-validation
  enable_cross_validation: true
  n_folds: 5
  
  # Out-of-sample testing
  out_of_sample_pct: 20.0  # Reserve 20% for final validation
  
  # Robustness checks
  robustness_tests:
    - "parameter_sensitivity"
    - "time_period_stability"
    - "market_regime_performance"

# Logging
logging:
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR
  save_logs: true
  log_file: "optimization_results/strategy_003/optimization.log"

# Strategy-Specific Notes
notes: |
  Strategy: HOD Continuation
  Category: 
  
  Building Blocks (5 total):
  - Hod (Weight: 20)
    * Above Hod []
  - Hod (Weight: 20)
    * Bullish []
  - Stochastic Rsi (Weight: 25)
    * No Cross []
  - Range Liquidity (Weight: 15)
    * Bullish []
  - How (Weight: 20)
    * Below How []
  
  Expected Frequency: Variable (depends on confluence threshold)
  Risk:Reward Target: 1:3
  
  Auto-generated configuration - review and adjust as needed.