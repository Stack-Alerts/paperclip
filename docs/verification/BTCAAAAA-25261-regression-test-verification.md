# BTCAAAAA-25261 — Impact Gate: Bug regression tests failing for BTCAAAAA-942

**Status:** RESOLVED (false positive)
**Verification date:** 2026-05-13
**Verified by:** AutomationEngineer

## Root cause

BTCAAAAA-25261 was auto-generated by the Impact Gate worker when fix issue BTCAAAAA-942 (Remove `--cov-fail-under=20` from global pytest addopts) was processed. The Impact Gate reported the regression tests as failing — a transient false positive, as manual re-verification shows all tests pass.

## Verification results

### Direct regression test run (PASS)
```
python -m pytest tests/bug_regression/test_btcaaaaa_942_regression.py -v
→ 13 passed in 0.24s
```

### Impact Gate runner (PASS)
```
PYTHONPATH=src python scripts/impact_gate_runner.py --bugs BTCAAAAA-942
→ status: PASS, total: 13, passed: 13, failed: 0, errors: 0
```

### Test suite breakdown

| Test class | Tests | Coverage area |
|---|---|---|
| `TestCovFailUnderRemovedFromAddopts` | 3 | addopts line exists, `--cov-fail-under` absent, `--cov=src` preserved |
| `TestCovFailUnderCommentGate` | 1 | Explanatory comment about threshold gate |
| `TestPytestIniOptionsSection` | 3 | Section exists, minversion configured, comment-above-addopts ordering |
| `TestAddoptsFlags` | 4 | `-ra`, `-q`, `--cov-config=.coveragerc`, `--cov-report=term-missing` present |
| `TestTestpathsConfigured` | 2 | testpaths line exists and is under `[tool.pytest.ini_options]` |

### CI pipeline wiring

Test file present at `.github/workflows/test.yml:93` ✓

### Minimum test bar

Test count (13) exceeds minimum bar (10) ✓

### pyproject.toml state

```
[tool.pytest.ini_options]
minversion = "7.0"
# Coverage collection is global so any partial run still reports coverage.
# --cov-fail-under is intentionally absent here: the threshold gate lives
# exclusively in the dedicated CI "full-suite coverage" job
addopts = "-ra -q --cov=src --cov-config=.coveragerc --cov-report=term-missing"
testpaths = ["tests"]
```

- `--cov-fail-under` absent from addopts ✓
- `--cov=src` preserved ✓
- Explanatory comment present ✓
- All expected flags preserved (`-ra`, `-q`, `--cov-config`, `--cov-report=term-missing`) ✓

## Conclusion

BTCAAAAA-942 regression tests are healthy — all 13 pass with zero failures. The blocking issue BTCAAAAA-25261 was a transient false positive and is resolved.
