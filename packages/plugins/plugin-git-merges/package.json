{
  "name": "@paperclipai/plugin-git-merges",
  "version": "0.1.0",
  "description": "Paperclip plugin that runs BTC-Trade-Engine-PaperClip's merge_queue_status.py script on a schedule and exposes a dashboard panel for the output under a 'Presets' sidebar section.",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "paperclipPlugin": {
    "manifest": "./dist/manifest.js",
    "worker": "./dist/worker.js",
    "ui": "./dist/ui/"
  },
  "scripts": {
    "prebuild": "pnpm --filter @paperclipai/plugin-sdk ensure-build-deps",
    "build": "pnpm --filter @paperclipai/plugin-sdk ensure-build-deps && pnpm exec esbuild src/worker.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/worker.js --external:electron --external:fsevents && tsc --emitDeclarationOnly --outDir dist && node ./scripts/build-ui.mjs",
    "clean": "rm -rf dist",
    "typecheck": "pnpm --filter @paperclipai/plugin-sdk ensure-build-deps && tsc --noEmit"
  },
  "dependencies": {
    "@paperclipai/plugin-sdk": "workspace:*",
    "@paperclipai/shared": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "@types/node": "^24.6.0",
    "@types/react": "^19.0.8",
    "@types/react-dom": "^19.0.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.7.3"
  },
  "peerDependencies": {
    "react": ">=18"
  }
}
