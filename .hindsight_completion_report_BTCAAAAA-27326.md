# BTCAAAAA-27326: Backup Dead-Man's-Switch Monitor

## Summary

Verified the backup pipeline liveness via `last-success.json`. Backup is healthy: last success at 2026-05-15 22:02 UTC (~1.5h ago), well under the 8h alert threshold.

## Verification Results

| Metric | Value |
|---|---|
| last-success.json | Found |
| Last backup success | 2026-05-15T22:02:25Z |
| Backup age | ~1.5h |
| Alert threshold | 8h |
| Status | HEALTHY |
| Systemd timer (backup-deadman-switch) | active |
| Systemd timer (backup-deadman-monitor) | active (84 runs) |
| Tests (backup_deadman_switch_monitor) | 61/61 passed |

## Monitoring Topology Verified

```
backup-to-drive (rclone) → last-success.json
  → backup-deadman-switch (systemd timer, every 30 min)
    → deadman-switch-monitor (GH Actions, ubuntu-latest, :15/:45)
      → backup-deadman-switch-monitor (systemd + GH Actions, self-hosted, :08/:38 + :12/:42)
```

## Retained

- Systemd timer at `:08/:38` offsets correctly from GH Actions schedule at `:12/:42`
- Systemd timer at `:03/:33` offsets for backup-deadman-switch (20 min before next backup window)
- Dual-source liveness: `gh run list` primary, `deadman_switch_monitor_state.json` fallback
- Alert deduplication: comments on existing alert instead of creating duplicates
- No API keys or credentials retained

## Disposition

Done.
