name: Touch Index FR Worker

on:
  schedule:
    # Every 15 minutes — process newly-updated FDR issues
    - cron: '*/15 * * * *'
  workflow_dispatch:
    inputs:
      lookback_minutes:
        description: 'Process FDR issues updated within this many minutes'
        required: false
        default: '30'

concurrency:
  group: touch-index-fr-worker
  cancel-in-progress: false

jobs:
  worker:
    name: Ingest FDR issue file references
    runs-on: ubuntu-latest

    env:
      PAPERCLIP_API_URL: ${{ secrets.PAPERCLIP_API_URL }}
      PAPERCLIP_API_KEY: ${{ secrets.PAPERCLIP_API_KEY }}
      PAPERCLIP_COMPANY_ID: ${{ secrets.PAPERCLIP_COMPANY_ID }}
      POSTGRES_HOST: ${{ secrets.POSTGRES_HOST || 'localhost' }}
      POSTGRES_PORT: ${{ secrets.POSTGRES_PORT || '5432' }}
      POSTGRES_DB: ${{ secrets.POSTGRES_DB || 'optimizer_v3' }}
      POSTGRES_USER: ${{ secrets.POSTGRES_USER || 'optimizer_admin' }}
      POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install requests python-dotenv SQLAlchemy psycopg2-binary

      - name: Run FR ingestion worker
        run: |
          if [ -n "${{ github.event.inputs.lookback_minutes }}" ]; then
            python scripts/run_touch_index_fr_worker.py \
              --lookback-minutes ${{ github.event.inputs.lookback_minutes }}
          else
            python scripts/run_touch_index_fr_worker.py
          fi
