# Git hooks

Bootstrap the version-controlled hooks in this worktree with `git config core.hooksPath .githooks`; the pre-commit hook then checks staged database migrations and their Drizzle journal entry.
